docker network create mon_reseau

docker run --name mongo --network mon_reseau -v db:/data/db -d mongo

docker run --name backend --network mon_reseau -p 5000:5000 -d mon-backend

Mon .env :

et ça c'est mon .env : PORT=5000
MONGODB_URI=mongodb://mongo:27017/flowersDB
